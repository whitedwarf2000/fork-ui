import SingleSlider from './SingleSlider';
import useSingleSlider from './useSingleSlider';

const Slider = f => f;

Slider.displayName = 'Slider';
Slider.Single = SingleSlider;
Slider.useSingleSlider = useSingleSlider;

export default Slider;
