import pushAlert from './Alert/push';
import pushNotification from './Notification/push';
import pushConfirm from './Confirm/push';

export default {
  pushAlert,
  pushNotification,
  pushConfirm,
};
